<div class="mb-3">
  <label for="{{fieldName}}" class="form-label"> {{fieldName.capitalizeAscii()}} </label>

  <input 
  type="text" 
  class="form-control" 
  id="{{fieldName}}" 
  form="form"
  name="{{fieldName}}"
  {% if isUpdateForm %} value="{{value}}" {% endif %}>

</div>